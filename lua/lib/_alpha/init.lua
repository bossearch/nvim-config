return {
    get_startup_time = require("lib._alpha.get_startup_time"),
    get_tips = require("lib._alpha.get_tips"),
    gh_contrib = require("lib._alpha.gh_contrib"),
    recent_files = require("lib._alpha.recent_files"),
    recent_project = require("lib._alpha.recent_project"),
}
