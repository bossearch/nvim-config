return {
    commits = require("lib._telescope.commits"),
    icons = require("lib._telescope.icons"),
    layout = require("lib._telescope.layout"),
    mapping = require("lib._telescope.mapping"),
}
